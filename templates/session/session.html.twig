{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <ul>Numéro de Session : {{ session.idSession }}
            <li>Heure ouverture : {{ session.heureDebut|date("H:i:s") }}</li>
            <li>Heure fermetuer : {{ session.heureFin|date("H:i:s") }}</li>
            <li>Utlisateur : {{ session.utilisateur }}</li>
            <li>Date : {{ session.date|date("Y-m-d") }}</li>
        </ul>
    </div>
{%  endblock %}
