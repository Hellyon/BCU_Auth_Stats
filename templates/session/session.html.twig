{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <li>Numéro de Session : {{ session.idSession }}</li>
            <ul>Heure ouverture : {{ session.heureDebut|date("H:i:s") }}</ul>
            <ul>Heure fermetuer : {{ session.heureFin|date("H:i:s") }}</ul>
            <ul>Utlisateur : {{ session.utilisateur }}</ul>
            <ul>Date : {{ session.date|date("Y-m-d") }}</ul>
    </div>
{%  endblock %}
